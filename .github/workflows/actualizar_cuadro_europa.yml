name: Actualizar cuadro de mando Europa

on:
  schedule:
    # Doble disparo UTC: ~21:10 hora Madrid en verano (19:10 UTC) y en invierno (20:10 UTC).
    # El correo llega siempre a las 21:00; solo ejecutamos después (ventana hora más abajo).
    # Lun–Sáb (domingo no se programa).
    - cron: '10 19 * * 1-6'
    - cron: '10 20 * * 1-6'
  workflow_dispatch:
    inputs:
      force_run:
        description: "Permite forzar la ejecución (siempre corre el extractor)"
        required: false
        default: "true"

env:
  TZ: Europe/Madrid

jobs:
  actualizar:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Instalar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Comprobar hora España (tras llegada del mail ~21:00)
        id: hora
        shell: bash
        run: |
          HM=$(date +%H%M)
          echo "Hora local Madrid: $(date '+%H:%M') (HM=$HM)"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Correo a las 21:00: ejecutar solo 21:05–21:59 (margen cola GitHub / reloj)
          if [ "$HM" -ge 2105 ] && [ "$HM" -le 2159 ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Ejecutar extractor
        if: steps.hora.outputs.run == 'true'
        env:
          TZ: Europe/Madrid
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          ASUNTO_FILTRO: ${{ secrets.ASUNTO_FILTRO }}
          REMITENTE: ${{ secrets.REMITENTE }}
          HOJA_EXCEL: ${{ secrets.HOJA_EXCEL }}
          NOMBRE_ADJUNTO: ${{ secrets.NOMBRE_ADJUNTO }}
          DRIVE_SPREADSHEET_ID: ${{ secrets.DRIVE_SPREADSHEET_ID }}
          DRIVE_TAB: ${{ secrets.DRIVE_TAB }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: python extractor_europa_actions.py

      - name: Commit y push si hay cambios
        if: steps.hora.outputs.run == 'true'
        shell: bash
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add data.json
          if git diff --staged --quiet; then
            echo "No hay cambios en data.json."
            exit 0
          fi

          git commit -m "Europa: datos $(date '+%Y-%m-%d %H:%M') — auto" || true

          # Reintentos por colisiones
          for i in 1 2 3; do
            git fetch origin main
            git rebase origin/main || (git rebase --abort && exit 1)
            git push origin HEAD:main && exit 0 || sleep 10
          done
          exit 1
